<%- include('../partials/header'); -%>

<div class="ui main text container segment">
    <div class="ui huge header">Editar Comentario al edificio <%= buildingVar.title %></div>
        <form class="ui form" action="/buildings/<%= buildingVar._id %>/comments/<%= commentVar._id %>?_method=PUT" method="POST">
            <div class="field">
                <label>Texto:</label>
                <input type="text" name="comment[text]" value="<%= commentVar.text %>">
            </div>
            <input class = "ui basic violet button" type="submit">
        </form>
</div>

<%- include('../partials/footer'); -%>